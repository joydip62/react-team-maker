
const Cart = ({ selectedTeams }) => {
//   console.log(selectedTeams);
  return (
    <div className="container">
      <h4>Total actor: {selectedTeams.length}</h4>
    </div>
  );
};


export default Cart;